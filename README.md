# Digital Product (Auth + Book + Runtimes)

This directory contains the deployable app:
- Traefik reverse proxy + TLS
- OTP email login (Mailgun) and forwardAuth
- Jupyter Book static site
- JupyterLab and Voilà services
- Redis queue + worker for emails/events
- CI workflow to build events-api image to GHCR

## Quick start (local dev)

Moved items to /Users/savantlab/Savantlab/digital_product:

## Deploy (server)


New structure under :
